#pragma once

#include <string>
#include <map>
#include <vector>

#include <microtar.h>

std::string base2_offshore(
    const std::string &,
    const std::string &);
std::string base2_offshore(
    const std::string &,
    const std::string &,
    const std::string &,
    const std::string &);