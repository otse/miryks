#pragma once

#include <string>
#include <map>
#include <vector>

#include <microtar.h>

std::string base2_offshore(
    const std::string &a,
    const std::string &b) {
        return base2_offshore(a, b, "", "");
    }
std::string base2_offshore(
    const std::string &a,
    const std::string &b,
    const std::string &c,
    const std::string &d) {

    }