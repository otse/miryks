#include "bsa"

#include <vector>
#include <fstream>

using namespace std;

struct asset_t {
	fle_t *fle;
};

struct arc_t {
	bsa_t bsa;
};

api arc_t arc_load(const string &);
void arc_init(arc_t &);
