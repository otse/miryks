#ifndef OPENGL_TYPES
#define OPENGL_TYPES

#include "../idiom.hpp"

#include <glad/glad.h>
#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>
using namespace glm;

struct aabb;

struct Geometry_t;
struct Material_t;
struct Camera_t;
struct Scene_t;
struct Group_t;
struct Texture_t;
struct Shader_t;
struct Program_t;
struct Pointlight_t;

extern Camera_t *camera;
extern Scene_t *scene;

extern Shader_t *sha_basic;
extern Shader_t *sha_simple;
extern Shader_t *sha_fx;
extern Shader_t *sha_pbr;

void detect_glfw_error();
void detect_opengl_error(const string = "?");

void opengl_go();
void opengl_reload_shaders();

#endif