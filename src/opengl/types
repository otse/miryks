#ifndef OPENGL_TYPES
#define OPENGL_TYPES

#include "../idiom.hpp"

#include <glad/glad.h>
#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>
using namespace glm;

struct aabb;

struct geometry_t;
struct material_t;
struct camera_t;
struct scene_t;
struct group_t;
struct texture_t;
struct shader_t;
struct program_t;
struct pointlight_t;

extern camera_t *camera;
extern scene_t *scene;

extern shader_t *sha_basic;
extern shader_t *sha_simple;
extern shader_t *sha_fx;
extern shader_t *sha_pbr;

void detect_glfw_error();
void detect_opengl_error(const char * = "?");

void opengl_go();
void opengl_reload_shaders();

#endif