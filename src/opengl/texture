#ifndef OPENGL_TEXTURE_H
#define OPENGL_TEXTURE_H

#include "types"

// https://learnopengl.com/Getting-started/Textures

struct Texture_t
{
    friend Texture_t *GetTexture(const string &);

    GLuint tid;

    const string path;

protected:
    Texture_t(const string &);
    ~Texture_t();

    void Load();
};

#endif